version: '3.8'

services:
  bot:
    build: .
    container_name: utyapal_bot
    restart: always
    network_mode: "host"
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
